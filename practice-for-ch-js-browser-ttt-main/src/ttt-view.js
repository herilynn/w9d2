class View {
  constructor(game, el) {
    this.game = new Game();
    this.el = el
  }
  
  setupBoard() {
  }
  
  handleClick(e) {
  }

  makeMove(square) {
  }
  
  handleGameOver() {
  }
}

export default View;